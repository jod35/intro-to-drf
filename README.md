# APIs With Django
This is simple code and short notes to learn Django API development with the Django REST Framework

## How to run the project

Install all requirements with 
```
pip install -r requirements.txt
```

Then run the server with 
```
mkdocs serve -a localhost:8003 --livereload
```